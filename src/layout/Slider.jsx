import React from 'react';
import PropTypes from 'prop-types';

const Slider = props => {
    return (
        <div>
            
        </div>
    );
};

Slider.propTypes = {
    
};

export default Slider;